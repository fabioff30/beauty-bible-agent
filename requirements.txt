# Beauty Bible Agent - Dependencies

# Telegram Bot Framework
python-telegram-bot>=20.0

# Async HTTP client
httpx>=0.25.0

# Environment variables
python-dotenv>=1.0.0

# Image processing (for basic skin analysis fallback)
Pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Storage (SQLCipher: encrypted SQLite)
# sqlcipher3-wheels = community-maintained binary wheels for macOS arm64 + Linux
# Module name on import remains `sqlcipher3` — same API as sqlcipher3-binary.
sqlcipher3-wheels>=0.5.0
aiosqlite>=0.19.0

# Scheduler for sleep-time consolidation jobs
APScheduler>=3.10.0

# Optional: OpenAI (if not using OpenRouter)
# openai>=1.0.0

# Development
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
